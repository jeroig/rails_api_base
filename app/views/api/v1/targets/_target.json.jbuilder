json.extract! target, :id, :user_id,
              :topic_id, :title,
              :latitude, :longitude,
              :radius, :created_at, :updated_at
# json.url target_url(target, format: :json)
# json.address address,
